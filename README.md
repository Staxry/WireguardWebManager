## WireGuard Web Management Interface

Современный веб‑интерфейс на Flask для управления WireGuard VPN‑сервером с акцентом на безопасность, аудит и удобство администрирования.  
Проект предназначен для развёртывания на Linux‑серверах и удобной выдачи/контроля клиентских конфигураций.

---

## 🚀 Основные возможности

1. **🔐 Система безопасности**
   - Хеширование паролей (PBKDF2)
   - Защита от брутфорс‑атак и блокировка IP
   - Таймаут сессий, смена пароля администратора

2. **📝 Аудит и логирование**
   - Полное логирование действий администратора
   - Статистика по операциям и IP‑адресам

3. **🛡️ Rate limiting**
   - Ограничение частоты запросов (web и API)
   - Блокировка при превышении лимитов

4. **💾 База данных**
   - SQLite для клиентов, сессий, трафика и настроек

5. **📡 Управление клиентами WireGuard**
   - Создание/удаление клиентов
   - Генерация конфигов и QR‑кодов
   - Просмотр статистики и трафика

6. **📊 Dashboard**
   - Сводка по подключениям и трафику
   - Системная информация

7. **🌙 Темная/светлая тема**
   - Переключатель темы, сохранение в `localStorage`

8. **⚡ REST API**
   - Получение/создание/удаление клиентов
   - Статистика и логи для интеграций

---

## 📋 Требования

### Системные требования
- **ОС**: Ubuntu 20.04+, Debian 11+, CentOS/Rocky 8+
- **Python**: 3.8+
- **WireGuard**: установленный и настроенный сервер
- **Права**: root/sudo для установки и управления сервисом

### Python‑зависимости
Основной список ‒ в `requirements.txt`:
- Flask 2.3+
- qrcode, Pillow (генерация QR‑кодов и QR‑изображений)
- Стандартная библиотека (sqlite3, subprocess и т.п.)

---

## 🔧 Установка и запуск

Ниже описан простой сценарий установки на сервере Linux.

### 1. Клонирование репозитория

```bash
git clone https://github.com/Staxry/WireguardWebManager.git
cd VPNServerControl
```

### 2. Установка системных пакетов (Ubuntu/Debian)

```bash
sudo apt update
sudo apt install python3 python3-pip python3-venv nginx sqlite3
```

### 3. Создание виртуального окружения и установка зависимостей

```bash
python3 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
```

### 4. Конфигурация администратора

Модуль `wg_web.admin_config` использует конфиг администратора (логин, хеш пароля, секретный ключ и параметры безопасности).  
Примерная структура файла (расположение можно настроить в `wg_web/admin_config.py`):

```json
{
  "username": "admin",
  "password_hash": "salt:hash",
  "secret_key": "your-secret-key",
  "session_timeout": 600,
  "max_login_attempts": 5,
  "lockout_duration": 300
}
```

Файл с конфигурацией **не должен попадать в Git** (см. `.gitignore`), храните его, например, в `/etc/wireguard-web/admin_config.json` с правами `600`.

### 5. Запуск для разработки

```bash
source .venv/bin/activate
python app.py
```

По умолчанию приложение слушает `http://127.0.0.1:5000`. Для удалённого доступа рекомендуется использовать Nginx как reverse‑proxy с HTTPS.

### 6. Запуск в продакшене (systemd)

В репозитории есть пример юнита `wireguard-web.service` и скрипт управления `wireguard-web-manager.sh`.  
Пример базовой настройки:

```bash
sudo cp wireguard-web.service /etc/systemd/system/wireguard-web.service
sudo systemctl daemon-reload
sudo systemctl enable wireguard-web
sudo systemctl start wireguard-web
sudo systemctl status wireguard-web
```

Далее настройте Nginx как reverse‑proxy к `127.0.0.1:5000` с SSL (Let’s Encrypt или свой сертификат).

---

## 🚀 Возможности (подробно)

### ✅ Реализованные функции

1. **🔐 Система безопасности**
   - Аутентификация с хешированием паролей (PBKDF2)
   - Защита от брутфорс атак
   - Блокировка IP адресов
   - Управление сессиями с таймаутом
   - Смена паролей администратора

2. **📝 Аудит и логирование**
   - Полное логирование всех действий пользователей
   - Отслеживание входов/выходов
   - Логирование операций с клиентами
   - Экспорт логов в различных форматах
   - Статистика по действиям

3. **🛡️ Rate Limiting**
   - Ограничение частоты запросов
   - Защита от DDoS атак
   - Автоматическая блокировка при превышении лимитов
   - Гибкая настройка лимитов для разных типов операций

4. **💾 База данных**
   - SQLite для хранения данных клиентов
   - Отслеживание трафика
   - История подключений
   - Настройки системы

5. **📊 Dashboard и мониторинг**
   - Интерактивный dashboard с графиками
   - Мониторинг системных ресурсов
   - Статистика по клиентам и трафику
   - Топ клиентов по использованию
   - Последние события системы

7. **🌙 Темная тема**
   - Переключение между светлой и темной темами
   - Сохранение предпочтений пользователя
   - Адаптивный дизайн

8. **⚡ API**
   - RESTful API для всех операций
   - Аутентификация через токены
   - Документация API
   - Rate limiting для API запросов

### 🎯 Основные функции управления

- **Управление клиентами**: Добавление, удаление, просмотр клиентов WireGuard
- **QR коды**: Автоматическая генерация QR кодов для мобильных устройств
- **Конфигурации**: Скачивание конфигурационных файлов
- **Мониторинг**: Отслеживание статуса подключений и трафика
- **Безопасность**: Многоуровневая система защиты

## 📋 Требования

### Системные требования
- **ОС**: Ubuntu 20.04+, Debian 11+, CentOS 8+, Rocky Linux 8+
- **Python**: 3.8+
- **WireGuard**: Установленный и настроенный сервер
- **Права**: Root доступ для установки

### Зависимости
- Python 3.8+
- Flask 2.3+
- Nginx (для продакшена)
- SQLite3
- OpenSSL

## 🔧 Установка

### Автоматическая установка

1. **Скачайте проект**:
```bash
git clone <repository-url>
cd VPNServerControl
```

2. **Запустите скрипт установки**:
```bash
sudo ./install.sh
```

Скрипт автоматически:
- Установит все зависимости
- Создаст пользователя для приложения
- Настроит Nginx с SSL
- Создаст systemd сервис
- Инициализирует базу данных
- Настроит файрвол

### Ручная установка

1. **Установите зависимости**:
```bash
# Ubuntu/Debian
sudo apt update
sudo apt install python3 python3-pip python3-venv nginx expect sqlite3

# CentOS/Rocky Linux
sudo yum install python3 python3-pip nginx expect sqlite
```

2. **Создайте директории**:
```bash
sudo mkdir -p /opt/wireguard-web
sudo mkdir -p /var/log/wireguard-web
sudo mkdir -p /etc/wireguard-web
```

3. **Скопируйте файлы**:
```bash
sudo cp -r . /opt/wireguard-web/
sudo chown -R www-data:www-data /opt/wireguard-web
```

4. **Установите Python зависимости**:
```bash
cd /opt/wireguard-web
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
```

5. **Настройте конфигурацию**:
```bash
# Создайте конфигурационный файл
sudo cp .admin_config.json /etc/wireguard-web/admin_config.json
sudo chmod 600 /etc/wireguard-web/admin_config.json
```

## ⚙️ Конфигурация

### Основная конфигурация

Файл `/etc/wireguard-web/admin_config.json`:
```json
{
  "username": "admin",
  "password_hash": "salt:hash",
  "secret_key": "your-secret-key",
  "session_timeout": 600,
  "max_login_attempts": 5,
  "lockout_duration": 300
}
```

### Настройки Rate Limiting

В файле `rate_limiter.py`:
```python
RATE_LIMITS = {
    'login': {'requests': 5, 'window': 300},      # 5 попыток за 5 минут
    'api_general': {'requests': 100, 'window': 60},  # 100 запросов в минуту
    'api_write': {'requests': 20, 'window': 60},     # 20 операций записи в минуту
    'web_general': {'requests': 200, 'window': 60},  # 200 веб-запросов в минуту
    'password_change': {'requests': 3, 'window': 3600},  # 3 смены пароля в час
}
```

### Настройка Nginx

Пример конфигурации в `/etc/nginx/sites-available/wireguard-web`:
```nginx
server {
    listen 443 ssl http2;
    server_name your-domain.com;
    
    ssl_certificate /path/to/certificate.crt;
    ssl_certificate_key /path/to/private.key;
    
    location / {
        proxy_pass http://127.0.0.1:5000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}
```

## 🚀 Запуск

### Разработка
```bash
cd /opt/wireguard-web
source venv/bin/activate
python app.py
```

### Продакшен
```bash
# Запуск сервиса
sudo systemctl start wireguard-web
sudo systemctl enable wireguard-web

# Проверка статуса
sudo systemctl status wireguard-web
```

## 📖 Использование

### Веб-интерфейс

1. **Откройте браузер** и перейдите по адресу: `https://your-server-ip`

2. **Войдите в систему**:
   - Логин: `admin`
   - Пароль: `admin123` (по умолчанию)

3. **Смените пароль** в разделе "Настройки"

### Основные функции

#### Добавление клиента
1. Перейдите в раздел "Добавить клиента"
2. Введите имя клиента
3. Нажмите "Создать"
4. Скачайте конфигурацию или отсканируйте QR код

#### Управление клиентами
- Просмотр списка всех клиентов
- Удаление клиентов
- Скачивание конфигураций
- Просмотр статистики подключений

#### Dashboard
- Мониторинг активных подключений
- Статистика трафика
- Системная информация
- График активности

### API

#### Аутентификация
```bash
curl -X POST https://your-server/api/v1/auth/login \
  -H "Content-Type: application/json" \
  -d '{"username": "admin", "password": "your-password"}'
```

#### Получение списка клиентов
```bash
curl -X GET https://your-server/api/v1/clients \
  -H "Authorization: Bearer your-token"
```

#### Добавление клиента
```bash
curl -X POST https://your-server/api/v1/clients \
  -H "Authorization: Bearer your-token" \
  -H "Content-Type: application/json" \
  -d '{"name": "client-name"}'
```

## 🔒 Безопасность

### Рекомендации по безопасности

1. **Смените пароль по умолчанию** сразу после установки
2. **Используйте HTTPS** с валидным SSL сертификатом
3. **Настройте файрвол** для ограничения доступа
4. **Регулярно обновляйте** систему и зависимости
5. **Мониторьте логи** на предмет подозрительной активности

### Настройка SSL

Для продакшена рекомендуется использовать сертификат от доверенного CA:

```bash
# Let's Encrypt
sudo certbot --nginx -d your-domain.com

# Или загрузите свой сертификат
sudo cp your-certificate.crt /etc/nginx/ssl/
sudo cp your-private.key /etc/nginx/ssl/
```

### Файрвол

```bash
# UFW
sudo ufw allow 22/tcp
sudo ufw allow 80/tcp
sudo ufw allow 443/tcp
sudo ufw allow 51820/udp  # WireGuard
sudo ufw enable

# iptables
sudo iptables -A INPUT -p tcp --dport 443 -j ACCEPT
sudo iptables -A INPUT -p tcp --dport 80 -j ACCEPT
sudo iptables -A INPUT -p udp --dport 51820 -j ACCEPT
```

## 📊 Мониторинг и логи

### Логи приложения
```bash
# Системные логи
sudo journalctl -u wireguard-web -f

# Логи аудита
sudo tail -f /var/log/wireguard-web/audit.log

# Логи rate limiting
sudo tail -f /var/log/wireguard-web/blocked_ips.json
```

### Мониторинг производительности
```bash
# Статус сервисов
sudo systemctl status wireguard-web nginx

# Использование ресурсов
htop
df -h
free -h
```

## 🛠️ Разработка

### Структура проекта
```
VPNServerControl/
├── app.py                 # Основное приложение Flask
├── admin_config.py        # Управление конфигурацией администратора
├── audit_log.py          # Система аудита и логирования
├── rate_limiter.py       # Rate limiting и защита от атак
├── database.py           # Работа с базой данных
├── api.py               # RESTful API
├── templates/           # HTML шаблоны
│   ├── base.html
│   ├── login.html
│   ├── index.html
│   ├── dashboard.html
│   └── ...
├── static/             # Статические файлы (CSS, JS)
├── requirements.txt    # Python зависимости
├── install.sh         # Скрипт установки
└── README.md          # Документация
```

### Добавление новых функций

1. **Создайте новый модуль** в корневой директории
2. **Импортируйте в app.py** с обработкой ошибок
3. **Добавьте маршруты** с соответствующими декораторами
4. **Обновите API** если необходимо
5. **Добавьте тесты** для новой функциональности

### Тестирование

```bash
# Установка зависимостей для тестирования
pip install pytest pytest-flask coverage

# Запуск тестов
pytest

# Покрытие кода
coverage run -m pytest
coverage report
```

## 🐛 Устранение неполадок

### Частые проблемы

#### Приложение не запускается
```bash
# Проверьте логи
sudo journalctl -u wireguard-web -n 50

# Проверьте права доступа
sudo chown -R wireguard-web:wireguard-web /opt/wireguard-web
sudo chmod +x /opt/wireguard-web/app.py
```

#### Ошибки базы данных
```bash
# Пересоздайте базу данных
cd /opt/wireguard-web
source venv/bin/activate
python -c "from database import init_database; init_database()"
```

#### Проблемы с SSL
```bash
# Проверьте сертификаты
sudo nginx -t
sudo openssl x509 -in /etc/nginx/ssl/wireguard-web.crt -text -noout
```

#### Rate limiting слишком строгий
```bash
# Отредактируйте настройки в rate_limiter.py
sudo nano /opt/wireguard-web/rate_limiter.py
sudo systemctl restart wireguard-web
```

### Логи и диагностика

```bash
# Все логи приложения
sudo journalctl -u wireguard-web --since "1 hour ago"

# Логи Nginx
sudo tail -f /var/log/nginx/access.log
sudo tail -f /var/log/nginx/error.log

# Проверка портов
sudo netstat -tlnp | grep -E "(80|443|5000)"

# Проверка процессов
ps aux | grep -E "(python|nginx|wireguard)"
```

## 📞 Поддержка

### Получение помощи

1. **Проверьте документацию** в этом README
2. **Изучите логи** для выявления ошибок
3. **Создайте issue** в репозитории с подробным описанием проблемы

### Сообщение об ошибках

При сообщении об ошибке укажите:
- Версию ОС
- Версию Python
- Логи ошибок
- Шаги для воспроизведения
- Ожидаемое поведение






 
